package sample;

import javafx.geometry.Insets;
import javafx.scene.layout.Background;
import javafx.scene.layout.BackgroundFill;
import javafx.scene.layout.CornerRadii;
import javafx.scene.paint.Color;

public class Backgrounds {
    public static Background saddlebrown = new Background(new BackgroundFill(Color.SADDLEBROWN, CornerRadii.EMPTY,
            Insets.EMPTY));
    public static Background peru = new Background(new BackgroundFill(Color.PERU, CornerRadii.EMPTY,
            Insets.EMPTY));
    public static Background tan = new Background(new BackgroundFill(Color.TAN, CornerRadii.EMPTY, Insets.EMPTY));
    public static Background green = new Background(new BackgroundFill(Color.GREEN, CornerRadii.EMPTY, Insets.EMPTY));
    public static Background yellow = new Background(new BackgroundFill(Color.YELLOW, CornerRadii.EMPTY, Insets.EMPTY));
    public static Background crimson = new Background(new BackgroundFill(Color.CRIMSON, CornerRadii.EMPTY, Insets.EMPTY));
    public static Background navy = new Background(new BackgroundFill(Color.NAVY, CornerRadii.EMPTY, Insets.EMPTY));



    /*private final Background burlywood = new Background(new BackgroundFill(Color.BURLYWOOD, CornerRadii.EMPTY,
            Insets.EMPTY));
    private final Background saddlebrown = new Background(new BackgroundFill(Color.SADDLEBROWN, CornerRadii.EMPTY,
            Insets.EMPTY));
    private final Background beige = new Background(new BackgroundFill(Color.BEIGE, CornerRadii.EMPTY, Insets.EMPTY));
    private final Background tan = new Background(new BackgroundFill(Color.TAN, CornerRadii.EMPTY, Insets.EMPTY));*/
}
